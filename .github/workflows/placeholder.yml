name: CI placeholder

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "CI pipeline placeholder - ready for setup"
